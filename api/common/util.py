# The common directory would probably just contain a set of helper functions to
# fulfill common needs across your application. It could also contain, for
# example, any custom input/output types your resources need to get the # job
# done.


def calculator(x, y):
    return x + y